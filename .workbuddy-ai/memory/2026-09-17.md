# 2026-09-17

## Table UI Improvements (Task 2)
Applied consistent rounded table container + ID column fix across all Scam Intelligence GUI pages.

### Changes made
- **static/gui-base.css**:
  - `.table-container, .table-wrap`: removed negative margins, added `border: 1px solid #e2e8f0`, `border-radius: 12px`, `box-shadow: 0 1px 3px rgba(15,23,42,0.08)`, `margin: 6px 12px`, `width: calc(100% - 24px)`.
  - Per-page overrides (scraping/investment/social) updated to match.
  - Replaced old `min-width: 56px` first-child rules with a single `[data-col="id"]` rule: `min-width: 120px`, `padding-left: 16px`, `overflow: visible`, `white-space: nowrap`.
  - Dark theme border-color set to `!important` so it overrides the light border.

- **Template markup** (`data-col="id"` added to ID `<th>`/`<td>`):
  - `index.html`: scraping, investment, activity log
  - `website_directory.html`: main table + activity sub-table
  - `qc_gui.html`: ID header + cell
  - `dashboard_management.html`: static thead, JS DASHBOARD_HEAD_HTML, JS row-builder td

### Verification
- Playwright screenshots captured for all 11 pages at 1366x768.
- Computed-style audit confirmed:
  - Container: `border-radius: 12px`, `border: 1px solid rgb(226,232,240)`, `margin: 6px 12px`
  - ID cells: `min-width: 120px`, `padding-left: 16px`, `overflow: visible`
- Pages with no ID column (`scam_website_allotment`, `lunch_break`) were not affected.

## Table UI Revisions — Square Corners + Standardized Headers
Removed rounded corners and standardized all table headers to match the Scrapping Data page design.

### Changes made
- **static/gui-base.css**:
  - Changed `border-radius: 12px` → `0px` on `.table-container`, `.table-wrap`, and per-page overrides.
  - Added universal header standardization rule for `.table-container th, .table-wrap th`:
    - `background: #f8f9fa !important`
    - `color: #2c3e50 !important`
    - `font-weight: 600 !important`
    - `border-right: 1px solid #eee !important`
    - `border-bottom: none !important`
    - `text-transform: none !important`
  - Updated dark theme header rules to match: `background: #334155`, `color: #e5e7eb`.

### Verification
- Computed-style audit confirmed all 8 data-table pages now have:
  - Container `border-radius: 0px`
  - Header `background: rgb(248, 249, 250)`, `color: rgb(44, 62, 80)`, `font-weight: 600`
- Previously divergent pages (investment orange, QC green, dashboard/allotment dark blue) now all match the scraping page header style.

## Table UI Revisions — Full-Width Flat Tables
Changed table containers to span full page width with no card/boxed appearance.

### Changes made
- **static/gui-base.css**:
  - `.table-container, .table-wrap`: changed `width: calc(100% - 24px)` → `calc(100% + 30px)`, `margin: 6px 12px` → `0 -15px` (full bleed like filters).
  - Removed `border`, `box-shadow`, and `background` from container — table cells provide their own visual structure.
  - Per-page overrides (scraping/investment/social) updated to match.
  - Dark theme container: removed `border-color`, kept `background: #1e293b`.

### Verification
- Computed-style audit confirmed:
  - Container: `width: 1366px` (or `calc(100% + 30px)`), `margin: 0px -15px`, `border: 0px none`, `border-radius: 0px`
  - Headers remain standardized: `background: rgb(248, 249, 250)`, `color: rgb(44, 62, 80)`
  - ID cells remain fully visible: `min-width: 120px`, `padding-left: 16px`

## Table UI Fixes — Investment & Social Inline Styles
Fixed remaining inline style conflicts in `index.html` that were overriding the shared `gui-base.css`.

### Changes made
- **templates/index.html**:
  - `.investment-page .table-container`: removed `width: 100%`, added explicit full-width styles (`width: calc(100% + 30px)`, `margin: 0 -15px`, no border/shadow/radius).
  - `.social-page .table-container`: same full-width fix.
  - `.investment-page th, .investment-page td`: added `overflow: visible; text-overflow: clip` to prevent text clipping.
  - `.investment-page th:first-child, .investment-page td:first-child`: added `min-width: 80px; padding-left: 16px` for ID column.
  - `.social-page th, .social-page td`: added `overflow: visible; text-overflow: clip`.
  - `.social-page th[data-col="id"], .social-page td[data-col="id"]`: added `min-width: 80px; padding-left: 16px` for ID column.
  - Updated CSS cache-busting version to `v=20260917-layout5`.

### Verification
- Playwright computed-style audit confirmed:
  - Investment table container: `x: 0, width: 1366px` (full viewport width)
  - Investment ID th: `width: 120px, minWidth: 120px, paddingLeft: 16px, text: 'ID'`
  - Social table container: `x: 0, width: 1366px` (full viewport width)
  - Social ID th: `width: 120px, minWidth: 120px, paddingLeft: 16px, text: 'ID'`
- All three pages (scraping, investment, social) now have identical table layout behavior.
